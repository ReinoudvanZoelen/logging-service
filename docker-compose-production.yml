version: "3"

services:
  logging:
    image: yoksar/logging-service
    container_name: "logging-service"
    # command: java -jar /usr/src/target/logging-service-1.0.0.jar
    ports:
      - 20090:8080